/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // domains allow images.unsplash.com to be used in next/image
  images: {
    domains: ["images.unsplash.com"],
  },
};

export default nextConfig;
